import styled from '@emotion/styled';
import { _white } from '@k-disciplines/core/src/theme/colors';

export const FormStyled = styled.form({
  width: '480px',
  border: 'none',
  borderRadius: '8px',
  padding: '20px 20px',

  backgroundColor: _white,
  boxShadow: '',
});
